stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: suite-cloud-maven
      pushRemote: true
      registry: 10.0.0.203:5000
timeout: 60
notification: {}
