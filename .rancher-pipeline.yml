stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: wujiaxin159/suite-cloud-maven
      pushRemote: true
      registry: 127.0.0.1:34889
timeout: 60
notification: {}
