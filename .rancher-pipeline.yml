stages:
- name: Publish
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: suite-cloud/suite-cloud-maven
      pushRemote: true
      registry: http://10.0.0.205:5000
timeout: 60
notification: {}
